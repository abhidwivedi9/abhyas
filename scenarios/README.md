# scenarios/
The product: the incident & exercise catalog (~220 target, see §7.3 of the
architecture doc). `incidents/` for pager-driven scenarios, `tickets/` for
project-style work. Author new scenarios from `TEMPLATE/` per
`docs/scenario-authoring.md`.
